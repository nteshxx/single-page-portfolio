import NavBar from '../Navbar/Navbar';
import Hero from '../Hero/Hero';
import './Header.css';

const Header = () => {
  return (
    <div id="header">
      <NavBar/>
      <Hero/>
    </div>
  );
}

export default Header;